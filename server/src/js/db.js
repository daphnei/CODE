// Generated by CoffeeScript 1.7.1
(function() {
  var Q, connectAndQuery, connection, getQuestionsFromTable, mysql;

  mysql = require('mysql');

  Q = require('Q');

  connection = mysql.createConnection({
    host: 'sql3.freemysqlhosting.net',
    user: 'sql330935',
    password: 'fW2!cZ8%',
    database: 'sql330935'
  });

  connectAndQuery = function(query, queryArgs) {
    var data;
    if (queryArgs == null) {
      queryArgs = [];
    }
    data = Q.defer();
    connection.query(query, queryArgs, function(err, rows, fields) {
      if (err) {
        return data.reject(err);
      } else {
        return data.resolve(rows);
      }
    });
    return data.promise;
  };

  exports.getMostDifficultQuestions = function(limit) {
    var queryString;
    queryString = 'SELECT * FROM questions WHERE num_answers >= 5 ORDER BY average_score LIMIT ?';
    return connectAndQuery(queryString, [limit]);
  };

  getQuestionsFromTable = function(name, limit) {
    var queryString;
    queryString = 'SELECT * FROM ?? LIMIT ?';
    return connectAndQuery(queryString, [name, limit]);
  };

  exports.getQuestions = function(type, limit) {
    return getQuestionsFromTable(type + '_questions', limit);
  };

}).call(this);
